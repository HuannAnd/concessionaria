version: "3"

services:
  # # mongodb:
  # next-app:
  #   container_name: next-app
  #   image: next-app-image
  #   build:
  #     context: ./frontend
  #     dockerfile: dev.Dockerfile
  #   ports:
  #     - 3000:3000
  #   restart: always
  backend:
    container_name: spring-boot
    build:
      context: ./backend/$JAR_NAME
      dockerfile: dev.Dockerfile
    ports:
    - 8080:8080
    environment:
      - JAR_NAME=concessionaria-backend
